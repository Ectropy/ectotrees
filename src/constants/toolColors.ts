// Moved to shared-browser/toolColors.ts so the Alt1 plugin can import it
// through the @shared-browser alias instead of reaching into src/.
// This shim keeps the ~25 existing dashboard imports working.
export * from '@shared-browser/toolColors';
